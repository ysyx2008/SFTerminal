---
description: Spec 驱动开发：设计目标优先于实现细节；讨论确认后先写 SPEC 再编码
alwaysApply: true
---

# Spec 驱动开发

模块目录下的 `SPEC.md`（或 `*_SPEC.md`）是模块的首要入口。SPEC 的核心不是 API 清单，而是**用户在讨论中确认的设计目标与取舍**。

## SPEC 分层（优先级从高到低）

1. **设计目标**（最重要）：要解决什么问题、成功标准、关键取舍、明确不做的事——来自与用户的讨论，**不是**从代码反推
2. **行为契约**：对外可见行为、不变量、边界条件
3. **实现索引**（可随代码更新）：公开 API、文件列表、类型引用——不得覆盖或稀释上一层

## 流程

- **讨论对齐后、动手改代码前**：把已确认的设计思路写入对应 SPEC 的「设计目标」；未写入不得开始实现
- **改动前**：先读 SPEC，尤其是「设计目标」——实现不得悄悄违背
- **实现偏离设计时**：先与用户对齐并更新 SPEC，再改代码；禁止只改代码不改目标
- **改动后**：若公开 API / 职责 / 约束变化，同步更新契约与实现索引；`Last verified` 日期
- **新增模块**：先有「设计目标」，再写代码；完成后补齐契约与索引（全文约 50–150 行，类型指向定义处而非复制）

## 禁止

- 用从代码反推的 API 表冒充「设计目标」
- 把易变实现细节（函数体步骤、临时命名）写进设计目标
- 讨论里定下的取舍只留在对话里、不落盘
