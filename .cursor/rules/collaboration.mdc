---
description: 人机协作开发规范，定义 AI Agent 在开发过程中的行为要求
alwaysApply: true
---

# 协作开发规范

用户负责：需求定义、架构决策、验收测试。AI 负责：编码、测试、代码审查。

## 本机工具

- **GitHub CLI (`gh`)**：可使用。

## 开发流程

1. **先理解再动手**：改动前充分阅读相关代码上下文（含对应 SPEC 的设计目标）
2. **大改动先出方案**：涉及 3 个以上文件的改动，先说明方案（改哪些文件、怎么改、影响什么），用户确认后再编码。大改动的方案落盘为 `docs/plans/<主题>-design.md`（plan 文档，实现细节的合法住所：类名/接口/数据模型随意用）；小改动对话确认即可，不必落盘
3. **设计先落 SPEC 再编码**：讨论中用户提出并确认的设计思路（目标、取舍、不做的事）必须先写入对应 SPEC 的「设计目标」，**然后才动手改代码**。对话里的共识不算数，SPEC 里的才算数
4. **不确定就问**：需求有歧义或有多种实现方式时，先问清楚，不要猜

## 改动完成后必须做

1. **跑测试**：至少跑 `bash electron/cli/test-cli.sh --no-ai`，涉及具体服务的用 cli-testing 技能查对应命令
2. **确认测试真的测到了**：回归保的是「原来能用的没坏」，新写的对不对是另一件事，而下面三种情况都会让人以为验过了。① **绿了不等于对**——断言只查你想到要查的，日志里的警告、对不上的数、多出来的行，都要追到明白为止。② **没红过的测试不算测试**——把修复拿掉跑一遍确认它会失败，否则它可能是被别的代码路径顺手补救而通过的，你以为在测 A，其实在测 B。③ **真实数据和真实进程造不出来**——动存储格式就拿真实数据副本逐条比对，碰并发就起真进程；同进程内的两个实例验不到操作系统层面的交错。
3. **代码审查**：用 claude-review 技能进行审查，发现问题自行修复
4. **变更摘要**：改动完成后给用户简明总结 — 改了哪些文件、做了什么、有什么需要注意的
5. **架构文档同步**：新增服务或重大重构后更新 project-architecture.mdc

## 大功能额外要求

- 拆成多个 commit，每个 commit 可独立验证；大 plan 在文档中先列「任务拆解」（原子任务 + 每步验收方式），commit 与任务对应
- 重构和新功能分开做，不要混在一起
- **plan 文档完成时两个动作**：① 讨论确认的稳定取舍升华进对应 SPEC 设计目标（转译为自然语言，去掉实现名）；② **删除 plan 实体文件**——plan 是一次性推导物，其价值已分别由 SPEC（取舍）和 git 历史（过程）承接，留在原地只会过期冒充现行设计；少数有长期架构参考价值的经用户确认后例外保留在 `docs/plans/`

## SPEC 文档要求

- **SPEC 记录用户的设计要求，不是实现索引**：SPEC 只记录讨论中确认的功能要求、设计取舍、行为承诺；禁止出现类名/方法名/API 表等代码实现内容（检验标准：每句话都应是用户能说出口、能看懂、能验收的话）。详见 `spec-driven.mdc`
- **改 service 前先读 SPEC.md**：改动任何 service 前，必须先读取对应的 `electron/services/XXX_SPEC.md`（或子目录 `SPEC.md`）。若不存在，先写设计目标再动手改代码
- **SPEC 只在讨论确认新设计决策时更新**：没有"改代码就要同步 SPEC"的义务；查代码结构用 CodeGraph，不靠 SPEC
- **探索必须用 CodeGraph**：摸底类层次/方法/接口/引用一律用 CodeGraph，**禁止直接 Read 整文件做"摸底"**
- **新 service 必须带 SPEC**：完成新 service 后必须有对应 SPEC.md（含设计目标），否则 PR/commit 不能过

## 禁止事项

- 不跑测试就说"完成了"
- 未经确认进行大范围重构
- 写基于关键词匹配的脆弱代码
- **给一个概念造第二个真相源**：多一处定义就多一个会各自演化的真相，日后必然对不上，而对不上那天没人会收通知。抄一遍是（前后端共用的类型一律放 `shared/types/`），换个词重新发明也是——已有联合类型（`TerminalType` / `ConversationKind` 等）就是分类的真相源，另起一套平行词汇再写翻译函数，兜底分支会把将来的新成员默默归错档。只有真正的新概念才配新建类型。
