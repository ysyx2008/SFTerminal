---
description: 人机协作开发规范，定义 AI Agent 在开发过程中的行为要求
alwaysApply: true
---

# 协作开发规范

用户负责：需求定义、架构决策、验收测试。AI 负责：编码、测试、代码审查。

## 本机工具

- **GitHub CLI (`gh`)**：可使用。

## 开发流程

1. **先理解再动手**：改动前充分阅读相关代码上下文（含对应 SPEC 的设计目标）
2. **大改动先出方案**：涉及 3 个以上文件的改动，先说明方案（改哪些文件、怎么改、影响什么），用户确认后再编码
3. **设计先落 SPEC 再编码**：讨论中用户提出并确认的设计思路（目标、取舍、不做的事）必须先写入对应 SPEC 的「设计目标」，**然后才动手改代码**。对话里的共识不算数，SPEC 里的才算数
4. **不确定就问**：需求有歧义或有多种实现方式时，先问清楚，不要猜

## 改动完成后必须做

1. **跑测试**：至少跑 `bash electron/cli/test-cli.sh --no-ai`，涉及具体服务的用 cli-testing 技能查对应命令
2. **代码审查**：用 claude-review 技能进行审查，发现问题自行修复
3. **变更摘要**：改动完成后给用户简明总结 — 改了哪些文件、做了什么、有什么需要注意的
4. **架构文档同步**：新增服务或重大重构后更新 project-architecture.mdc

## 大功能额外要求

- 拆成多个 commit，每个 commit 可独立验证
- 重构和新功能分开做，不要混在一起

## SPEC 文档要求

- **设计目标来自用户讨论，不是代码反推**：SPEC 最重要的部分是用户确认过的设计思路；API/文件表只是实现索引。详见 `spec-driven.mdc`
- **改 service 前先读 SPEC.md**：改动任何 service 前，必须先读取对应的 `electron/services/XXX_SPEC.md`（或子目录 `SPEC.md`）。若不存在，先写设计目标再动手改代码
- **探索必须用 code-query**：摸底类层次/方法/接口/引用一律用 `npx tsx scripts/code-query.ts`，**禁止直接 Read 整文件做"摸底"**
- **新 service 必须带 SPEC**：完成新 service 后必须有对应 SPEC.md（含设计目标），否则 PR/commit 不能过

## 禁止事项

- 不跑测试就说"完成了"
- 未经确认进行大范围重构
- 写基于关键词匹配的脆弱代码
- **重复定义类型**：前后端共用的类型必须定义在 `shared/types/` 中，不得在 `src/`、`electron/` 或 Vue 组件中重复定义相同的 interface/type。需要时通过 `import type { ... } from '@shared/types'` 导入
