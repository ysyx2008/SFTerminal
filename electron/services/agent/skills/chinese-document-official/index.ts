/**
 * 中国党政机关公文写作技能
 *
 * 提供 GB/T 9704-2012 公文体例知识 + official/securities/meeting 三个样式预设。
 * 引擎和工具仍在 word 技能中，本技能只贡献策略数据（写作规范 + 样式配置）。
 *
 * 适用场景：写公文、通知、请示、报告、纪要等中文正式文档时加载。
 */

import { registerSkill } from '../registry'
import type { Skill } from '../types'
import { registerStylePreset } from '../word/styles'
import { officialPreset, securitiesPreset, meetingPreset } from './presets'
import { createLogger } from '../../../../utils/logger'

const log = createLogger('ChineseDocumentOfficialSkill')

// 顶层注册样式预设（import 副作用，确保 markdownToDocx 能拿到这些预设）
registerStylePreset('official', officialPreset)
registerStylePreset('securities', securitiesPreset)
registerStylePreset('meeting', meetingPreset)

const chineseDocumentOfficialSkill: Skill = {
  id: 'chinese-document-official',
  name: '党政机关公文写作',
  description: '生成中文公文（通知、请示、报告、批复、函、纪要等）时使用，提供 GB/T 9704-2012《党政机关公文格式》的体例规范，并启用 official/securities/meeting 三个公文样式预设。',
  tools: [],
  content: [
    '## 中国党政机关公文写作（GB/T 9704-2012）',
    '',
    '**适用范围**：写中文公文（通知、请示、报告、批复、函、纪要等）以及证券公司公文、会议纪要时使用。本技能加载后启用 `official` / `securities` / `meeting` 三个样式预设。',
    '',
    '> 提示：写中文文档前请同时加载 `chinese-writing` 技能（GB/T 15834 标点 + GB/T 15835 数字），它是所有中文写作的基础规范。',
    '',
    '### 中文公文写作要点（GB/T 9704-2012）',
    '生成中文公文、报告、纪要、通知、请示等正式文档时，遵循 **GB/T 9704-2012《党政机关公文格式》** 的版式与文种规定。排版细节（字体、字号、版心、页边距、行距）由所选样式（official / securities / meeting / regulation）自动实现，AI 主要关注**写作内容本身**符合下述规范。',
    '',
    '**1. 公文标题三要素**：[发文机关] + 关于 + [事由] + [文种]',
    '- 完整示例：`国元证券股份有限公司关于进一步加强金融科技赋能业务发展的通知`',
    '- 标题中**不加书名号**，不加引号（如"关于《XX办法》的通知"中的《》是引用其他文件，不是给自己加书名号）',
    '- 标题在 front matter 的 `title` 字段中提供，会自动套 Title 样式（公文样式中为小标宋体二号居中）',
    '- 标题过长时不要在 title 里硬换行，由排版引擎处理；如必须分行，遵循"上长下短梯形"原则，在词义完整处断开',
    '',
    '**2. 法定文种共15种**（其他文种名称在公文中无效）：',
    '决议、决定、命令、公报、公告、通告、意见、通知、通报、报告、请示、批复、议案、函、纪要。',
    '不要使用"申请书""说明书""汇报""备忘录"等非法定文种作为公文文种（这些可作为日常文书，但不属于党政机关公文）。',
    '',
    '**3. 公文要素与典型结构**：',
    '```',
    '[版头：发文机关标志、文号、密级、紧急程度]      ← 由表单元数据或人工后期补充，AI 一般不写',
    '[主体]',
    '  标题（front matter title）',
    '  主送机关：xx，xx：                              ← 顶格写，冒号收尾',
    '  正文（缘由 → 事项 → 结尾用语）',
    '    结尾用语示例：',
    '      - 通知：请遵照执行 / 特此通知',
    '      - 请示：妥否，请批示 / 当否，请示',
    '      - 报告：特此报告 / 专此报告',
    '      - 函：请予函复 / 特此函达',
    '  附件说明：附件：1. xxx；2. xxx                  ← 如有附件',
    '  发文机关署名（右对齐，用 <p align="right">）',
    '  成文日期（用汉字数字写法：二〇二六年五月四日，按 GB/T 15835 公文场景例外）',
    '[版记：抄送、印发机关、印发日期]                ← 一般由办公室补全',
    '```',
    '',
    '**4. 主送机关写法**：顶格写、可并列（用顿号/逗号分隔），末尾用冒号。如 `各部门、各分支机构：`',
    '',
    '**5. 成文日期写法（特殊例外）**：公文落款的成文日期使用**汉字数字**，年份不简写不加"零"，例如 `二〇二六年五月四日`（不要写成"2026年5月4日"或"二零二六年五月四日"）。这是 GB/T 9704 对公文场景的明确规定，覆盖了 GB/T 15835 中"公历日期用阿拉伯数字"的通用规则。',
    '',
    '### 语义结构分析（重要）',
    '将内容转为正式文档（尤其是公文、报告）时，必须先通读全文，根据语义判断文档结构。切勿机械地按段落位置套用格式。',
    '',
    '**标题识别**：',
    '- 文档标题不一定是第一段。第一段可能是"附件1"、发文字号、密级等辅助信息',
    '- 真正的文档标题通常是概括全文主题的短句，应放在 front matter 的 title 中',
    '- 标题前后的辅助文字（如"附件1"、主送机关）用 <p> 或 <p align="center"> 处理',
    '',
    '**层级分配**：',
    '- front matter title = 文档标题（全文仅一个，Title 样式）',
    '- `#` = 大节标题 / Heading 1（公文里的"一、二、三、"章节）',
    '- `##` = 小节标题 / Heading 2（公文里的子项标题）',
    '- `###` = 三级标题',
    '',
    '**公文常见结构**（标题可能不在第一段的情况）：',
    '- 附件类：附件编号 → 文档标题 → 正文',
    '- 带文号：发文字号 → 文档标题 → 主送机关 → 正文',
    '- 带密级：密级/紧急程度 → 文档标题 → 正文',
    '',
    '### ⚠️ 公文章节编号必须用 `#` 显式标记（重要）',
    '`securities` / `official` / `meeting` 等公文样式中，"一、""（一）""1．"等中文编号文本本身**不会**自动升级为标题样式（这是有意的设计：避免"（一）组织保障。说明……"这种以编号开头的正文段被误判为标题）。',
    '',
    '**写法对照**：',
    '',
    '| 场景 | 正确写法 | 错误写法 |',
    '|------|---------|---------|',
    '| 章节标题（独立成行、概括下文） | `# 一、xxx` | `一、xxx`（不会成标题） |',
    '| 子项标题（独立成行、概括下文） | `## （一）xxx` | `（一）xxx`（不会成标题） |',
    '| 正文里的并列叙述（编号 + 短标题 + 句号 + 长说明） | 直接写正文段：`（一）组织保障。成立金融科技创新工作领导小组，由分管副总裁任组长……` | 不要在前面加 `##` |',
    '',
    '**判断要点**：',
    '- "（一）xxx"后面如果是**句号 + 一段说明文字**（典型如"组织保障。成立……"、"人才保障。加大……"），它是**正文的并列项**，不是子标题，**不要**加 `#`',
    '- "（一）xxx"如果**独占一行、概括下面一段或几段正文**，它是子标题，应该写 `## （一）xxx`',
    '- 整段如果同时存在多个"（一）（二）（三）"形式的并列项，且每项都是"短标题。长说明"结构，全部当作正文处理',
    '',
    '**通用原则**：标题层级由 markdown 的 `#` 数量决定（独立成行的章节标题用 `#`），正文不加 `#`。这跟标准 markdown 用法一致。',
  ].join('\n'),

  async init() {
    log.info('Initialized')
  },

  async cleanup() {
    log.info('Cleaned up')
  }
}

try {
  registerSkill(chineseDocumentOfficialSkill)
} catch (error) {
  log.error('Failed to register:', error)
}

export { chineseDocumentOfficialSkill }
