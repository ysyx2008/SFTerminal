/**
 * 中文写作规范技能
 *
 * 纯 content 技能（无工具），定义中文材料写作的国家标准规范：
 *  - GB/T 15834-2011《标点符号用法》
 *  - GB/T 15835-2011《出版物上数字用法》
 *  - 中文排版"汉字与数字/英文之间不加空格"惯例
 *
 * 适用所有产中文文本的场景（word / excel / email / im / chat 等），
 * 让规范脱离 word skill 独立存在，避免规则散落在多处。
 */

import { registerSkill } from '../registry'
import type { Skill } from '../types'
import { createLogger } from '../../../../utils/logger'

const log = createLogger('ChineseWritingSkill')

const chineseWritingSkill: Skill = {
  id: 'chinese-writing',
  name: '中文写作规范',
  description: '生成任何中文正式材料（公文、报告、合同、邮件、消息等）时使用，提供 GB/T 15834 标点符号、GB/T 15835 数字用法以及"汉字与数字/英文之间不加空格"等中文写作规范。',
  tools: [],
  content: [
    '## 中文写作规范（必读）',
    '',
    '**适用范围**：所有中文正式材料 — 公文、报告、纪要、通知、请示、合同、信函、邮件、消息等。本技能加载后，写中文文本时必须按此规范执行。',
    '',
    '### 标点符号（必须遵循 GB/T 15834-2011）',
    '',
    '凡是中文内容的文本，标点必须遵循 **国家标准 GB/T 15834-2011《标点符号用法》**，使用全角中文标点，不要保留 AI 默认的英文半角标点。常见映射：',
    '',
    '- 双引号：`"…"`（半角直引号）→ `\u201C…\u201D`（全角弯引号）',
    '- 单引号：`\'…\'` → `\u2018…\u2019`',
    '- 句号 `.` → `。`（句末）',
    '- 逗号 `,` → `，`',
    '- 冒号 `:` → `：`',
    '- 分号 `;` → `；`',
    '- 感叹号 `!` → `！`',
    '- 问号 `?` → `？`',
    '- 顿号（并列项之间）：用 `、`，不要用 `,`',
    '- 破折号：必须是 `\u2014\u2014`（两个 U+2014 字符位连用，占两格），不要用 `--`、`-`、单个 `\u2014` 或 `~`',
    '- 省略号：必须是 `\u2026\u2026`（两个 U+2026 三连点连用，共6个点占两格），不要用 `...`、单个 `\u2026` 或 `。。。`',
    '- 括号：用 `（）`，不要用 `()`',
    '- 书名号（引用文件、法规、书刊、规章等）：用 `《》`，例如"根据《XX公司采购管理办法》第三条规定"。**不要**用 `"…"`、`*斜体*` 或 markdown 链接代替。',
    '- 间隔号：人名、书名章节之间用 `·`（如"列奥纳多·达·芬奇"、"《史记·项羽本纪》"），不要用半角 `.`',
    '- 连字符：**标准号、文件号、版本号、邮编、电话、ISBN 等编号中的连字符必须是半角 `-`（U+002D）**，不要替换成破折号 `\u2014`、连接号 `\u2013` 或全角短横 `\uff0d`。',
    '  - ✅ 正确：`GB/T 15834-2011`、`GB/T 9704-2012`、`v1.2.3`、`010-12345678`、`100086-2000`、`ISO 9001-2015`',
    '  - ❌ 错误：`GB/T 15834\u20142011`（破折号）、`GB/T 15834\u20132011`（连接号）、`GB/T 15834\uff0d2011`（全角短横）',
    '  - 这些编号本质是"国际/国家通用标识符"，连字符是其规范的一部分，不能因为"中文公文要用全角"而过度修正。',
    '',
    '**引号嵌套规则**（GB/T 15834 第4.8.3节）：外层用双引号、内层用单引号，不能内外都用双引号。',
    '- 正确：`他说："你听过\'书读百遍其义自见\'这句话吗？"`',
    '- 错误：`他说："你听过"书读百遍其义自见"这句话吗？"`',
    '',
    '**句末点号不重复**：句末已有问号/叹号时，后面不再补句号；引号内已结句的，引号外不再加句号（即不要写"。""。"）。',
    '',
    '**例外**：以下场景保留英文半角标点：',
    '- 英文术语、专有名词内部（如 "U.S."、"e.g."、"Inc."）',
    '- 代码块、行内代码、URL、文件路径',
    '- 数字之间（如 "3.14"、"2026-05-04"、"v1.2.0"）',
    '- 公式、变量名',
    '',
    '### 数字用法（必须遵循 GB/T 15835-2011）',
    '',
    '中文公文、报告中的数字用法应遵循 **GB/T 15835-2011《出版物上数字用法》**：',
    '',
    '- **用阿拉伯数字**：公历年月日时刻（`2026年5月4日`、`14时30分`）、计量数据（`3.5万元`、`28.5米`）、统计表中的数据、编号代码（`国办发〔2024〕18号`）',
    '- **用汉字数字**：定型词语和成语（`三五成群`、`不二法门`）、概数（`三四十人`、`十几年`）、农历和清代以前历史日期（`正月初一`、`康熙五十六年`）、法规公文中的章条款项序数（`第三条`、`第二章第一节`、`（一）（二）`）',
    '- **不要混用**：同一类对象在同一文档中应保持一致（如全篇用阿拉伯数字写金额时，不要某处突然写"五万元"）',
    '',
    '### ⚠️ 数字两侧不要加空格（盘古之白反模式）',
    '',
    '中文文档中**汉字与数字、汉字与英文术语、数字与单位之间一律不加空格**。这违反 AI 受英文排版习惯影响产生的"盘古之白"行为模式（英文文档中"Section 3"、"version 1.0" 习惯空格隔开），但中文出版规范不允许。',
    '',
    '| 类别 | ✅ 正确 | ❌ 错误 |',
    '|------|---------|---------|',
    '| 日期 | `2026年5月4日` | `2026 年 5 月 4 日` |',
    '| 时刻 | `14时30分` | `14 时 30 分` |',
    '| 金额计量 | `3.5万元` `28.5米` `100公里` | `3.5 万元` `28.5 米` `100 公里` |',
    '| 序数量词 | `15种` `共8项` `第3批次` | `15 种` `共 8 项` `第 3 批次` |',
    '| 文号编号 | `国办发〔2024〕18号` `第AB-001号` | `国办发〔2024〕18 号` |',
    '| 中英文混排 | `安装Word软件` `使用GB/T 9704规范` | `安装 Word 软件`（中文与英文术语之间） |',
    '| 标准号本身 | `GB/T 15834-2011` | `GB / T 15834 - 2011` |',
    '',
    '⚠️ 注：序数量词举例避开 `第3条` `第3章` 等，因为法规公文中的章条款项序数按 GB/T 15835 必须用汉字数字（"第三条""第二章第一节"），与"序数量词无空格"是两个独立规则，不要混在一起举例。',
    '',
    '**例外（必须保留空格）**：英文术语内部按英文规范（如 `Section 3.2`、`version 1.0` 这类**纯英文短语**保留），代码、URL、文件路径、公式按其原始格式。但只要句子的整体语境是中文，与汉字相邻的数字/英文不加空格。'
  ].join('\n'),

  async init() {
    log.info('Initialized')
  },

  async cleanup() {
    log.info('Cleaned up')
  }
}

try {
  registerSkill(chineseWritingSkill)
} catch (error) {
  log.error('Failed to register:', error)
}

export { chineseWritingSkill }
