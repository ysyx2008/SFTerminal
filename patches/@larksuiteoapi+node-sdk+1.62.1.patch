diff --git a/node_modules/@larksuiteoapi/node-sdk/es/index.js b/node_modules/@larksuiteoapi/node-sdk/es/index.js
index 008f36c..764f776 100644
--- a/node_modules/@larksuiteoapi/node-sdk/es/index.js
+++ b/node_modules/@larksuiteoapi/node-sdk/es/index.js
@@ -85403,7 +85403,7 @@ class WSClient {
         return __awaiter(this, void 0, void 0, function* () {
             const { appId, appSecret } = this.wsConfig.getClient();
             try {
-                const { code, data: { URL, ClientConfig }, msg } = yield this.httpInstance.request({
+                const response = yield this.httpInstance.request({
                     method: "post",
                     url: this.wsConfig.wsConfigUrl,
                     data: {
@@ -85417,12 +85417,16 @@ class WSClient {
                     },
                     timeout: 15000,
                 });
+                const { code, data, msg } = response;
                 if (code !== ErrorCode.ok) {
                     this.logger.error('[ws]', `code: ${code}, ${code === ErrorCode.system_busy ? msg : 'system busy'}`);
-                    if (code === ErrorCode.system_busy || code === ErrorCode.internal_error) {
-                        return false;
-                    }
+                    return false;
+                }
+                if (!data || !data.URL || !data.ClientConfig) {
+                    this.logger.error('[ws]', 'invalid ws config response: missing URL or ClientConfig');
+                    return false;
                 }
+                const { URL, ClientConfig } = data;
                 const { device_id, service_id } = qs.parse(URL);
                 this.wsConfig.updateWs({
                     connectUrl: URL,
diff --git a/node_modules/@larksuiteoapi/node-sdk/lib/index.js b/node_modules/@larksuiteoapi/node-sdk/lib/index.js
index 39ac49b..e4893bc 100644
--- a/node_modules/@larksuiteoapi/node-sdk/lib/index.js
+++ b/node_modules/@larksuiteoapi/node-sdk/lib/index.js
@@ -85421,7 +85421,7 @@ class WSClient {
         return __awaiter(this, void 0, void 0, function* () {
             const { appId, appSecret } = this.wsConfig.getClient();
             try {
-                const { code, data: { URL, ClientConfig }, msg } = yield this.httpInstance.request({
+                const response = yield this.httpInstance.request({
                     method: "post",
                     url: this.wsConfig.wsConfigUrl,
                     data: {
@@ -85435,12 +85435,16 @@ class WSClient {
                     },
                     timeout: 15000,
                 });
+                const { code, data, msg } = response;
                 if (code !== ErrorCode.ok) {
                     this.logger.error('[ws]', `code: ${code}, ${code === ErrorCode.system_busy ? msg : 'system busy'}`);
-                    if (code === ErrorCode.system_busy || code === ErrorCode.internal_error) {
-                        return false;
-                    }
+                    return false;
+                }
+                if (!data || !data.URL || !data.ClientConfig) {
+                    this.logger.error('[ws]', 'invalid ws config response: missing URL or ClientConfig');
+                    return false;
                 }
+                const { URL, ClientConfig } = data;
                 const { device_id, service_id } = qs__default["default"].parse(URL);
                 this.wsConfig.updateWs({
                     connectUrl: URL,
