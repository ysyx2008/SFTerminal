/**
 * 样例业务工作台 → Agent prompt（无对话区；演示 descriptor.agentPrompt 装配）
 */
export const SAMPLE_WORKBENCH_AGENT_PROMPT = [
  '# 样例业务工作台（sample）',
  '',
  '这是 OEM / 业务条线的**最小工作台模板**，用于验证 monorepo 注册与 bootstrap。',
  '当前 UI 无对话区；正式岗位台可换成自研界面，或等 P2 后复用 AiPanel。',
].join('\n')
