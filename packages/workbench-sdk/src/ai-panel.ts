/**
 * 同款对话区正式出口（薄壳）。
 *
 * 实现仍在 desktop `src/components/AiPanel.vue`。
 * 工作台包必须从此处 import，禁止 `@/components/AiPanel`。
 * 不迁实现（W7b）；缺能力先加薄壳门牌，再引用。
 */
export { default as AiPanel } from '../../../src/components/AiPanel.vue'
